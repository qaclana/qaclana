apply plugin: 'war'

description = 'Example of how to consume the filter'

dependencies {
    compile project(':filter')

    providedCompile libraries['javaee']
    providedCompile libraries['jbossLogging']
}

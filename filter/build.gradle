apply plugin: 'java'

description = 'Filter components for Qaclana'

dependencies {
    compile project(':api')
    compile project(':processors/blocklist-processor')

    compile libraries['javaee']
    compile libraries['jbossLogging']
}

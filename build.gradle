apply plugin: 'java'

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.5'
}

ext {
    buildNumber = System.env.BUILD_NUMBER ? ".${System.env.BUILD_NUMBER}" : "-SNAPSHOT"
    version = "0.0.1${buildNumber}"
}


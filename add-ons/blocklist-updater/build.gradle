apply plugin: 'java'

description = 'Java EE Batch that updates the Blacklists'

dependencies {
    compile project(':shared')

    compile libraries['javaee']
    compile libraries['jbossLogging']
}


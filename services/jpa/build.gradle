apply plugin: 'java'
apply plugin: 'org.inferred.processors'

description = 'Services implemented with JPA as backend'

dependencies {
    compile project(':api')

    compile libraries['javaee']
    compile libraries['jbossLogging']
    processor libraries['jpaModelGenerator']
}
